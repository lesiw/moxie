# Simple example

This example provides a more concrete example of using moxie to mock a behaviour and then test something.
It is closer to a real case example than the Basic example. If you haven't already, make sure to check out basic example.

## How to run this example

```
go generate && go test ./...`
```
